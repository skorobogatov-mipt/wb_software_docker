# robot_software_docker

__Build docker container__:

`./build_container.sh`

__Run docker container__:

`./run_container.sh`

__Build project__:

`cd workspace && colcon build`

__Start walk tuner__:

`ros2 run controller publisher`
